import { Container } from '@mui/material';
import { useState } from 'react';

export const TestItems = ({ itemsArr, filterKey }) => {
  const [items, setItems] = useState([]);
  
  return (
      <Container maxWidth="xl">

      </Container>
  );
}
